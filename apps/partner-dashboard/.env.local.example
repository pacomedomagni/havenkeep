# API (server-side — used by middleware and server actions)
API_URL=http://localhost:3000

# API (client-side — used by browser fetch via Next.js rewrites)
NEXT_PUBLIC_API_URL=http://localhost:3000

# App
NEXT_PUBLIC_APP_URL=http://localhost:3001
