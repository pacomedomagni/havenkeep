---
// Testimonials section
const testimonials = [
  {
    quote: "My Samsung fridge compressor failed 2 weeks before the warranty expired. HavenKeep reminded me just in time\u2014saved us $1,800 on a replacement. Without it, I would have been out of luck.",
    name: "Sarah Mitchell",
    role: "Homeowner, Austin TX",
    saved: "$1,800",
    initials: "SM",
    gradient: "from-primary to-secondary",
  },
  {
    quote: "I manage 14 rental properties. Before HavenKeep, tracking warranties across 200+ appliances was a nightmare spreadsheet. Now it takes me zero effort. Last quarter alone, I caught 3 expiring warranties worth $4,200.",
    name: "Marcus Johnson",
    role: "Property Manager",
    saved: "$4,200",
    initials: "MJ",
    gradient: "from-secondary to-accent",
  },
  {
    quote: "My kid spilled juice on our new laptop. I totally forgot we had accidental damage coverage. HavenKeep had the receipt AND warranty doc ready. Filed the claim in 5 minutes and got a full replacement.",
    name: "Emily Chen",
    role: "Mom of 3, Seattle WA",
    saved: "$2,400",
    initials: "EC",
    gradient: "from-accent to-primary",
  },
];
---

<section class="py-24 bg-gray-900">
  <div class="container">
    <!-- Section Header -->
    <div class="max-w-3xl mx-auto text-center mb-16 reveal">
      <span class="text-accent font-semibold text-sm uppercase tracking-wider">Real Stories</span>
      <h2 class="text-4xl md:text-5xl font-bold text-white mt-3 mb-4">
        They saved thousands.<br />You can too.
      </h2>
      <p class="text-xl text-gray-300">
        Don&apos;t take our word for it&mdash;hear from people who stopped losing money.
      </p>
      <p class="text-sm text-gray-500 mt-2">Individual savings vary based on the items you track and warranty claims filed.</p>
    </div>

    <!-- Testimonial Cards -->
    <div class="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto">
      {testimonials.map((t, i) => (
        <div class={`reveal reveal-delay-${i + 1} relative bg-gray-800 rounded-2xl p-8 border border-gray-700 hover:border-primary/50 transition-all duration-300`}>
          <!-- Saved badge -->
          <div class="absolute -top-3 right-6">
            <span class={`bg-gradient-to-r ${t.gradient} text-white text-sm font-bold px-4 py-1.5 rounded-full shadow-lg`}>
              Saved {t.saved}
            </span>
          </div>

          <!-- Stars -->
          <div class="flex gap-1 mb-4 mt-2">
            {[1,2,3,4,5].map(() => (
              <svg class="w-5 h-5 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
              </svg>
            ))}
          </div>

          <!-- Quote -->
          <blockquote class="text-gray-300 leading-relaxed mb-6 text-sm">
            &ldquo;{t.quote}&rdquo;
          </blockquote>

          <!-- Author -->
          <div class="flex items-center gap-3 pt-4 border-t border-gray-700">
            <div class={`w-10 h-10 rounded-full bg-gradient-to-br ${t.gradient} flex items-center justify-center text-white font-bold text-sm`}>
              {t.initials}
            </div>
            <div>
              <p class="text-white font-semibold text-sm">{t.name}</p>
              <p class="text-gray-500 text-xs">{t.role}</p>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
