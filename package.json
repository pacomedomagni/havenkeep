{
  "name": "havenkeep",
  "version": "0.0.1",
  "private": true,
  "description": "HavenKeep — Your Warranties. Protected.",
  "scripts": {
    "dashboard:dev": "cd apps/partner-dashboard && npm run dev",
    "dashboard:build": "cd apps/partner-dashboard && npm run build",
    "db:migrate": "cd apps/api && npx ts-node src/db/migrations/run-migration.ts"
  }
}
